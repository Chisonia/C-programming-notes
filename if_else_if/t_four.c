#include <stdio.h>

void main(){
    int a, b, sum;
    printf("Enter one number: ");
    scanf("%d", &a);
    if (a){
        printf("a is: %d\n", a);
    }
    printf("Don't want to calculate sum");
}